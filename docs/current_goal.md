Work on the first goal that isn't marked complete:
- [] Phase 1 Task: Mock Reset Utility. Create `reset_mock_db.py` script to clear the mock environment created in Supabase Mock Client. Update tests accordingly.
