# Backend

FastAPI application serving REST and WebSocket endpoints for the Experiment Manager.

- `main.py` exposes the FastAPI `app`.
- `models.py` defines Pydantic models shared between the backend and UI.
