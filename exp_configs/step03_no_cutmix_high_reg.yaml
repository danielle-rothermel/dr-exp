_target_: "dr_exp.trainers.decon_trainer.train_classification"
# Step 03: Remove CutMix (2019)
# Building on step02, removing CutMix augmentation

defaults:
  - step02_no_randaug
  - _self_

# High regularization experiment overrides
optim:
  lr: 0.1
  weight_decay: 5e-4

train_transforms:
  cutmix: false  # Removed CutMix