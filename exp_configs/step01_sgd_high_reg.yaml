_target_: "dr_exp.trainers.decon_trainer.train_classification"
# Step 01: Switch from AdamW to SGD with Momentum
# First removal in the chronological experiment

defaults:
  - step00_baseline
  - override /optim: sgd  # Changed from adamw to sgd
  - _self_

# High regularization experiment overrides
optim:
  lr: 0.1
  weight_decay: 5e-4