_target_: "dr_exp.trainers.decon_trainer.train_classification"
# Step 05: Remove warmup
# Building on step04, removing learning rate warmup

defaults:
  - step04_no_mixup
  - _self_

run_name: step05_no_warmup
lrsched:
  warmup_epochs: 0  # Removed warmup
