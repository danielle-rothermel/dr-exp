_target_: "dr_exp.trainers.decon_trainer.train_classification"
# Step 16: Remove RRC (Random Resized Crop) augmentation
# Building on step15, removing spatial augmentation

defaults:
  - step15_no_colorjitter
  - _self_

run_name: step16_no_rrc
train_transforms:
  rcc: false  # Remove Random Resized Crop
