# Step 08: Switch from Batch Norm to Dropout + LRN
# Building on step07, replacing BatchNorm with Dropout (0.4) and LRN

defaults:
  - step07_no_residual
  - _self_

model:
  norm_type: lrn  # Switch from batchnorm to LRN
  dropout_prob: 0.4  # Add dropout before final linear layer
