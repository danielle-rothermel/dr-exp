_target_: "dr_exp.trainers.decon_trainer.train_classification"
# Step 04: Remove Mixup (2018)
# Building on step03, removing Mixup augmentation

defaults:
  - step03_no_cutmix
  - _self_

run_name: step04_no_mixup
train_transforms:
  mixup: false  # Removed Mixup
