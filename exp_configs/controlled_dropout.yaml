_target_: "dr_exp.trainers.decon_trainer.train_classification"
# Controlled experiment: Dropout regularization

defaults:
  - controlled_base
  - _self_

run_name: controlled_dropout

model:
  dropout_prob: 0.2  # Add moderate dropout