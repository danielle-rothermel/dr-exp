# Step 06: Switch to step LR decay (from cosine annealing)
# Building on step05, switching to older step-based LR scheduling

defaults:
  - step05_no_warmup
  - override /lrsched: torchvision_step  # Changed from timm_cosine to step LR
  - _self_

lrsched:
  warmup_epochs: 0  # Keep warmup disabled
