# Test Suite

Pytest tests for the Experiment Manager.

Subdirectories:
- `integration/` – end-to-end tests of the stack.
- `mock/` – tests for mock components.

Other `test_*.py` files cover individual modules such as the manager, logger, and API.
