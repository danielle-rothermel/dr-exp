# Frontend Source

React source code for the Babysitter UI.

Files include the entry point `main.jsx`, global styles, and helper modules. Components are organized in the `components/` folder and images/icons live in `assets/`.
