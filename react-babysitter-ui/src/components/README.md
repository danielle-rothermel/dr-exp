# React Components

Reusable components that make up the Babysitter UI.

- `JobTable.jsx` – displays a table of jobs pulled from the backend.
- `JobDetailView.jsx` – page for viewing a single job configuration.
