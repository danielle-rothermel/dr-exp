# Mock Database

Placeholder directory for local Supabase mock data.

During development the mock client stores JSON and JSONL files here to emulate Supabase tables and metrics. The actual data files are ignored via `.gitignore` to keep the repository clean.
