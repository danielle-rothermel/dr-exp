# Optimizer Configurations

Contains optimizer hyperparameters used by training runs.

- `adam.yaml`  – settings for the Adam optimizer.
- `adamw.yaml` – settings for AdamW.
- `sgd.yaml`   – settings for stochastic gradient descent.

These files are combined with other configs to build complete experiment setups.
