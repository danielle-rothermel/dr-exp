data: ${machine.root_dir}/data
logs: ${machine.root_dir}/logs

my_data: ${paths.data}/${..proj_dir_name}
my_logs: ${paths.logs}/${..proj_dir_name}

run_dir: ${paths.my_logs}/${now:%Y-%m-%d-%H-%M-%s}
dataset_cache_root: ${paths.data}/${data.name}/
agg_results: ${paths.data}/run_results/
