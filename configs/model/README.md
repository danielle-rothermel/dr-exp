# Model Configurations

YAML files specifying model architectures used in experiments.

- `resnet.yaml` – parameters for a ResNet-based model.
- `vit.yaml` – parameters for a Vision Transformer model.

Additional model definitions can be added here and referenced from the base `config.yaml`.
