# Supabase Project

Configuration files for the Supabase backend used by the Experiment Manager.

- `config.toml` defines the local Supabase project.
- `migrations/` contains SQL schema migrations.
- `.temp/` records the version of the Supabase CLI used when generating the project.
